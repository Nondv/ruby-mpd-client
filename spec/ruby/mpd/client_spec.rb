require "spec_helper"

describe Ruby::Mpd::Client do
  it "has a version number" do
    expect(Ruby::Mpd::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
